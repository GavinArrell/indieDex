var itemList = [];

$(document).ready(function() {
	
	$('.storeAddItem').on("click", function() {
		
		if($(this).innerHTML = "Add") {}
		var temp = $('storeAddItem').closest('storeItem').innerHTML;
		
	});
	
)};
